<div class="row">	
	<div class="col-md-12 sectioncontainer">
    <% if @contacts.any? -%>
      <% c = @contacts.order('id DESC').first %>
      <h3><b><%= c.title %></b></h3>
      <span><%= c.body.html_safe %></span>
      <br>
    <% end -%>
	</div>	
</div>

